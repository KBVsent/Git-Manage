[project]
name = "hoshino-module-git-manage"
version = "0.1.0"
description = "Git management utilities for HoshinoBot"
requires-python = ">=3.12"
dependencies = [
    "gitpython>=3.1.44",
]

[tool.uv]
package = false

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
